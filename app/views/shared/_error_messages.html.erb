<% if @study.errors.any? %>
  <div class="message-banner error-banner" id="error_explanation">
    <div class="banner-content">
      <p>
        <strong>The form contains <%= pluralize(@study.errors.count, "error") %>.</strong>
      </p>
      <ul>
        <% @study.errors.full_messages.each do |msg| %>
          <li><%= msg %>.</li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>