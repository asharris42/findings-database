<% if @activity_type.errors.any? %>
  <div class="message-banner error-banner" id="error_explanation">
    <div class="banner-content">
      <p>
        <strong>The form contains <%= pluralize(@activity_type.errors.count, "error") %>.</strong>
      </p>
      <ul>
        <% @activity_type.errors.full_messages.each do |msg| %>
          <li><%= msg %>.</li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>